import { GradeColorDirectiveDirective } from './grade-color-directive.directive';

describe('GradeColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GradeColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
