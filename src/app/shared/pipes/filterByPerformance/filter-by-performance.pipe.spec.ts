import { FilterByPerformancePipe } from './filter-by-performance.pipe';

describe('FilterByPerformancePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPerformancePipe();
    expect(pipe).toBeTruthy();
  });
});
